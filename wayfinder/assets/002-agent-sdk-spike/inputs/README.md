Drop screenshots here (`.png`, `.jpg`, `.webp`, `.gif`).

The spike picks up every image in this folder automatically and lists it as a
selectable input. Two or three shots of designs you actually like is enough -
the point is to judge whether the analysis captures what you like about them or
produces generic filler.

These files are prototype scratch. Nothing here is committed to main.
